{
  "name": "@muze-nl/jsfs",
  "version": "0.3.5",
  "author": "auke@muze.nl",
  "main": "src/index.mjs",
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "description": "A filesystem abstraction for remote storage",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-dev": "esbuild --bundle src/index.mjs --outfile=dist/browser.js --sourcemap;",
    "build": "esbuild --bundle src/index.mjs --outfile=dist/browser.min.js --minify --sourcemap;"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/muze-nl/jsfs.git"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/muze-nl/jsfs/issues"
  },
  "homepage": "https://github.com/muze-nl/jsfs#readme",
  "dependencies": {
    "@muze-nl/metro": "^0.6.15",
  }
}
